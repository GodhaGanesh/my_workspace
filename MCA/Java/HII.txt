CREATE TABLE CUSTOMERS1(
CUSTOMERID NUMBER(3) PRIMARY KEY,
FIRST_NAME VARCHAR2(20) NOT NULL,
LAST_NAME VARCHAR2(10) NOT NULL,
COUNTRY VARCHAR2(10)
);

INSERT INTO CUSTOMERS1
VALUES(&CUSTOMERID,'&FIRST_NAME','&LASTNAME','&COUNTRY');

UPDATE CUSTOMERS1
SET STATE = 'ANDHRA PRADESH'
WHERE COUNTRY IN ('INDIA','BHARATH');

ALTER TABLE CUSTOMERS1
ADD EMAIL VARCHAR(30);

UPDATE CUSTOMERS1
SET EMAIL = CONCAT(FIRST_NAME,CONCAT(LAST_NAME,'@GMAIL.COM'))
WHERE FIRST_NAME IS NOT NULL;

UPDATE CUSTOMERS1
SET COUNTRY = 'INDIA'
WHERE COUNTRY= 'BHARATH';